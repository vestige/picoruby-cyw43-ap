# 開発・検証の基本方針

この文書は、`picoruby-cyw43-ap` と必要に応じてPicoRuby Coreをまたぐ作業に共通する活動方針です。個別Issueの実装内容、期待出力、検証結果は各Issueや作業記録に残し、この文書には固定しません。

## 対話と作業範囲

- 作業内容、確認結果、次の段階を日本語で説明し、ユーザーが確認しながら一つずつ進める。
- 外部mrbgemのIssueとPull Requestは日本語、PicoRuby CoreのIssueとPull Requestは英語で記載する。
- コミットメッセージは英語で記載する。
- Issueごとに目的、対象外、成功条件、必要なボード・VM構成を明確にする。隣接する問題を無断で同じ作業に広げない。
- 予想と異なる結果は成功扱いにせず、観測事実と推測を分けて説明する。

## Gitとファイルの保護

- 作業開始時と重要な段階で、対象リポジトリのbranch、remote、status、未コミット差分、untracked files、最近のcommitを読み取り専用で確認する。
- Issueごとにブランチを分ける。
- 既存の変更、ファイル、履歴を勝手に破棄、巻き戻し、上書きしない。
- `git reset --hard` とforce pushは行わない。
- PicoRuby Coreの通常checkoutにあるtracked filesは変更しない。Coreの修正とビルドは専用の一時worktreeで行う。
- 外部mrbgemをCoreの一時worktreeから参照する際のsymlinkや一時build configはcommit対象に含めない。Coreの標準build configも変更しない。
- commit前にstatus、diff、`git diff --check`、追跡対象を再確認する。credentials、秘密情報、生成物、UF2、ビルドディレクトリ、一時ファイルを含めない。
- commit、push、Pull Request作成は、それぞれユーザーの明示的な指示を受けてから行う。

## 検証の進め方

1. 作業対象と成功条件を定め、変更範囲や既存コードとの整合性を確認する。必要なら型検査や静的確認を行う。
2. 関連するホストテストを実行する。回帰テストを追加した場合はその結果を含め、成功数、failure、exceptionを記録する。失敗を解消してから実機へ進む。
3. 対象ボードとVMごとにファームウェアをビルドする。外部mrbgemが実際に組み込まれたことをログで確認し、UF2の絶対パス、サイズ、SHA-256を記録する。
4. 必要な実機テストを行い、Issueで決めた成功条件を直接確認する。ホストテストの成功だけで実機の成功とは判断しない。
5. 変更差分と検証結果をユーザーへ示し、内容確認を受けてから次のGit操作へ進む。

## 実機への書き込み

- ユーザーが対象ボードを明示し、BOOTSEL状態にしたと伝えるまでUF2を書き込まない。
- 書き込み直前に外部ボリュームと `INFO_UF2.TXT` を読み取り専用で確認する。対象デバイスを一意に識別できなければ書き込まない。
- UF2が対象ボード・VM向けの今回のビルドであることを確認してからコピーし、同期する。
- コピー後にBOOTSELボリュームが消え、通常起動したことを確認する。単なる再起動をファームウェア更新と取り違えない。

## 実機テストと後始末

- 使用中のシリアルポートがないことを確認してから接続し、テスト後はセッションを終了してポートを解放する。
- テストを始める条件、操作するタイミング、期待出力、終了後の状態を個別Issueに合わせて事前に定める。
- 表示がない場合も成功とは判断せず、状態確認など別の観測方法で結果を確かめる。
- テストで有効にしたAPや開いたsocketなどは、失敗時も可能な範囲で停止・解放し、後始末の結果を伝える。
- 想定外の結果になったら、実際に書き込まれたUF2、Core、外部mrbgem、テストコードを切り分ける。原因を確認してから修正・再検証する。

## 完了前の確認

- Issueで必要としたボード・VM構成と成功条件をすべて確認する。
- コード、テスト、コメント、Issue・Pull Requestの説明が、観測した原因と実際の修正内容に一致することを確認する。
- 未検証の範囲、残るリスク、次の作業を明示する。
